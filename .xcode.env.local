# Local overrides for Xcode bundling script
# Force the build phase to run bundling when building from Xcode
SKIP_BUNDLING=0
# Ensure the entry file is the virtual metro entry used by expo-dev-client
ENTRY_FILE=.expo/.virtual-metro-entry
# Use Expo CLI's embed command (this is default but explicit here)
BUNDLE_COMMAND=export:embed
